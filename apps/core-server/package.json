{
  "name": "@incidents/core-server",
  "version": "0.0.1",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "@incidents/auth": "workspace:*",
    "@incidents/database": "workspace:*",
    "@incidents/queue": "workspace:*",
    "@incidents/common": "workspace:*",
    "date-fns": "^4.1.0",
    "ioredis": "^5.6.1",
    "nanoid": "^5.1.5",
    "pino": "^9.8.0",
    "pino-pretty": "^13.1.1",
    "radash": "^12.1.1",
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
