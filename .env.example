# =============================================================================
# 🚀 Incidents Management System - Environment Configuration Template
# =============================================================================
# 这是环境变量配置模板文件
# 复制此文件为 .env 并根据你的实际环境修改相应的值
# =============================================================================

# -----------------------------------------------------------------------------
# 🗄️ 数据库配置 (Database Configuration)
# -----------------------------------------------------------------------------
# PostgreSQL 数据库连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名?schema=模式名
DATABASE_URL="postgresql://postgres:password@localhost:5432/incidents?schema=public"

# -----------------------------------------------------------------------------
# 🔄 Redis 配置 (Redis Configuration)
# -----------------------------------------------------------------------------
# Redis 连接字符串，用于消息队列和缓存
# 格式: redis://主机:端口
REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# 🌐 服务器端口配置 (Server Port Configuration)
# -----------------------------------------------------------------------------
# REST API 服务器端口
REST_SERVER_PORT=3000

# 核心服务器端口
CORE_SERVER_PORT=3001

# Web 前端开发服务器端口
WEB_DEV_PORT=5173

# -----------------------------------------------------------------------------
# 🔐 认证配置 (Authentication Configuration)
# -----------------------------------------------------------------------------
# Better Auth 密钥 (用于JWT签名等)
# 生产环境请使用强随机字符串，建议使用 openssl rand -base64 32 生成
BETTER_AUTH_SECRET="your-super-secret-key-change-in-production"

# Better Auth URL (应用的基础URL)
BETTER_AUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# 👤 初始管理员配置 (Initial Admin Configuration)
# -----------------------------------------------------------------------------
# 初始管理员邮箱地址
INIT_ADMIN_EMAIL="admin@localhost"

# -----------------------------------------------------------------------------
# 🌍 应用环境配置 (Application Environment)
# -----------------------------------------------------------------------------
# 应用运行环境: development | production | test
NODE_ENV="development"

# 应用名称
APP_NAME="Incidents Management System"

# 应用版本
APP_VERSION="0.0.1"

# -----------------------------------------------------------------------------
# 📧 邮件配置 (Email Configuration) - 可选
# -----------------------------------------------------------------------------
# SMTP 服务器配置 (如果需要邮件通知功能)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM="noreply@yourdomain.com"

# -----------------------------------------------------------------------------
# 🔔 通知配置 (Notification Configuration) - 可选
# -----------------------------------------------------------------------------
# Webhook URL (如果需要外部通知)
# WEBHOOK_URL="https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK"

# -----------------------------------------------------------------------------
# 📊 监控配置 (Monitoring Configuration) - 可选
# -----------------------------------------------------------------------------
# 日志级别: error | warn | info | debug
LOG_LEVEL="info"

# 是否启用详细日志
VERBOSE_LOGGING="false"

# -----------------------------------------------------------------------------
# 🐳 Docker 配置 (Docker Configuration)
# -----------------------------------------------------------------------------
# 以下配置用于 Docker 部署时的数据库和 Redis 连接
# DOCKER_DATABASE_URL="postgresql://incidents_user:incidents_password@postgres:5432/incidents"
# DOCKER_REDIS_URL="redis://redis:6379"