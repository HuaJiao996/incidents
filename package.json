{
  "name": "incidents",
  "version": "1.0.0",
  "description": "Incidents management system",
  "type": "module",
  "scripts": {
    "db:install": "node scripts/setup.js install",
    "db:start": "node scripts/setup.js start",
    "install:prod": "cross-env NODE_ENV=production npm install"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "cross-env": "^7.0.3",
    "execa": "^9.5.2"
  }
}
